# Pendientes tecnicos

- Confirmar si `descripcion_motivo` debe quedarse en `varchar(255)` o volver a
  `varchar(100)` cuando se valide la homogeneizacion final del DER con el
  catalogo oficial de motivos.
