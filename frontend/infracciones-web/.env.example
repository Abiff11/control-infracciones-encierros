# En desarrollo usa el proxy de Vite para mantener frontend y API en el mismo
# origen visible al navegador. Esto permite que la cookie CSRF double-submit
# sea legible por el frontend aunque el backend escuche en localhost:3000.
VITE_API_URL=/api
